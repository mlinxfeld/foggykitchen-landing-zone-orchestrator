# Scripts

This directory is reserved for helper scripts, validation helpers, or payload tooling in later phases.

The Azure MVP keeps runtime logic inside Terraform/OpenTofu and uses only a minimal cloud-init script colocated with the example that consumes it.
